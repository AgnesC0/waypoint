# Waypoint runtime dependencies
# tkinter ships with CPython — no install needed.
# macOS: lsof and osascript are system utilities — no install needed.
# Windows: see requirements-windows.txt for additional dependencies.

PyYAML>=6.0
